class Dish {
  String title;
  String description;
  double price;
  double rating;
  String image;
  bool isFavorite;

  Dish(this.title, this.description, this.price, this.image, this.rating,
      this.isFavorite);
}
